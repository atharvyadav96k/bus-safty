name: Monorepo Deploy

on:
  push:
    branches: [ main ]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.filter.outputs.server }}
      notify: ${{ steps.filter.outputs.notify }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            server:
              - 'dogzh-server/**'
            notify:
              - 'notify-function/**'

  deploy-server:
    needs: changes
    if: needs.changes.outputs.server == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-run.yml
    with:
      working-directory: dogzh-server
      gcp_region: "us-central1"
      deploy-type: "run"
    secrets: inherit

  deploy-notify:
    needs: changes
    if: needs.changes.outputs.notify == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-run.yml # Reusing the same logic or a separate one
    with:
      working-directory: notify-function
      gcp_region: "us-central1"
      deploy-type: "function"
    secrets: inherit